server:
  port: 7979

eureka:
    instance:
        hostname: localhost
    clien:
        registerWithEureka: false
        fetchRegistry: false
        service-url:
            default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
